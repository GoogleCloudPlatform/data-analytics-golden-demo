# requirements.in
# pip install pip-tools
# pip-compile --generate-hashes --index-url=https://pypi.org/simple requirements.in

google-adk==2.1.0
json-stream==2.3.3
tenacity==9.1.4

# Add greenlet explicitly because it's a platform-specific
# dependency of sqlalchemy, which is required by google-adk.
greenlet
# GitHub Dependent a Bot!  Security issue.
authlib==1.6.12